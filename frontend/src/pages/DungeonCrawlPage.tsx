import { CharacterBank } from 'components/crawl/CharacterBank';

export default function DungeonCrawlPage() {
    return (
        <div className="flex h-full flex-col bg-gray-900">
            <CharacterBank />
            {/* Battle section placeholder — to be built next */}
        </div>
    );
}
